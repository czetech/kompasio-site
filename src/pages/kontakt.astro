---
import { getEntry } from "astro:content";
import BaseLayout from "~/layouts/BaseLayout.astro";
import ArrowDown from "@lucide/astro/icons/arrow-down";
import Mail from "@lucide/astro/icons/mail";
import Circle from "~/components/Circle.tsx"

const contact = await getEntry("contact", "contact");
---

<BaseLayout>
  <div class="flex gap-x-4 items-center p-8 justify-center">
    <Circle>
      <ArrowDown />
    </Circle>
    <h1 class="font-semibold text-2xl md:text-3xl">{contact.data.title}</h1>
  </div>
  <div class="grow p-8 flex justify-center flex-col items-center">
    <div class="bg-vibrant-blue p-6 max-w-144 w-full rounded-2xl flex flex-col gap-y-16 text-shuttle-white">
      <h2 class="text-xl md:text-2xl font-semibold">{contact.data.cardTitle}</h2>
      <div class="flex flex-col gap-y-4">
        <p>{contact.data.emailLabel}</p>
        <a href={`mailto:${contact.data.emailAddress}`} class="border rounded-full flex self-start items-center gap-x-4 pr-4">
          <Mail class="p-1.5 w-8 h-8 border rounded-full" />
          <span>{contact.data.emailAddress}</span>
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
