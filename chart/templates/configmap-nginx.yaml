apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kompasio-site.fullname" . }}-nginx
  labels:
    {{- include "kompasio-site.labels" . | nindent 4 }}
data:
  nginx.conf: |-
{{ .Files.Get "docker-nginx.conf" | indent 4 }}
